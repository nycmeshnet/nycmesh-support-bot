name: Manual live deploy

on:
  workflow_dispatch

jobs:
  Run:
    runs-on: [self-hosted, daniel-server]
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "💡 The repository has been cloned to the runner."
      - name: Build and run tests 🧪
        run: |
          docker stop nycmesh-support-bot || true
          docker rm nycmesh-support-bot || true
          docker build -t nycmesh-support-bot .
          docker run -d --name nycmesh-support-bot -v "$PWD/.env:/app/.env" --restart unless-stopped andybaumgar/nycmesh-support-bot:$GITHUB_SHA supportbot-server --channel-ids "C045QM9TDMZ" --nn-property-id "XfBTK3QZ1S"